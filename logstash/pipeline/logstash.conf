input {
 beats {
     port => 5044
   }
}
filter {
    mutate {
        remove_field => [ "agent", "ecs", "input", "log", "tags","stream", "host" ]
    }
}
output {
     elasticsearch {
         hosts => ["http://elasticsearch:9200"]
         user => "elastic"
         password => "changeme"
         index => "%{[@metadata][beat]}-%{[@metadata][version]}-%{+YYYY.MM.dd}"
     }
     stdout {
         codec => rubydebug
     }
}