filebeat.inputs:
    - type: docker
      paths:
        - '/var/lib/docker/containers/*/*.log'
      json.message_key: log
      json.keys_under_root: true
output.logstash:
    hosts: ["localhost:5044"]
